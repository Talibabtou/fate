{
  "name": "@fate/app",
  "private": true,
  "type": "module",
  "scripts": {
    "keeper": "node --experimental-strip-types scripts/keeper.ts",
    "keeper:once": "node --experimental-strip-types scripts/keeper.ts --once",
    "hrr:bind": "node --experimental-strip-types scripts/hrr-bind.ts",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test scripts/*.test.ts"
  },
  "dependencies": {
    "@noble/hashes": "^2.3.0",
    "@solana/kit": "^7.1.1",
    "@solana/kit-plugin-rpc": "^0.17.0",
    "@solana/kit-plugin-signer": "^0.17.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
